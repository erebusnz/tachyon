# DAC8552 -- 16-Bit Dual-Channel Ultralow Glitch Voltage Output DAC

## BOM Details

| Field | Value |
|---|---|
| MPN | DAC8552IDGKR |
| Manufacturer | TI |
| Package/Footprint | VSSOP-8_L3.0-W3.0-P0.65-LS5.0-BL |
| Designator | U6 |
| Quantity | 1 |
| Supplier Part | C136020 (LCSC) |

## Description

The DAC8552 is a 16-bit, dual-channel, voltage output digital-to-analog converter from Texas Instruments. It features an on-chip output buffer amplifier providing rail-to-rail output swing, a double-buffered input architecture, and an ultralow glitch energy of 0.15 nV-s. The device communicates via a 3-wire SPI-compatible serial interface supporting clock rates up to 30 MHz and powers on with outputs at zero-scale.

## Key Electrical Characteristics

| Parameter | Min | Typ | Max | Unit | Condition |
|---|---|---|---|---|---|
| Resolution | -- | 16 | -- | bits | -- |
| Integral Nonlinearity (INL) | -- | -- | +/-12 | LSB | VDD = 5.5 V |
| Differential Nonlinearity (DNL) | -- | -- | +/-1 | LSB | Guaranteed monotonic |
| Relative Accuracy | -- | -- | 4 | LSB | -- |
| Output Voltage Range | 0 | -- | VDD | V | Rail-to-rail |
| Settling Time (to +/-0.003% FSR) | -- | 10 | -- | us | -- |
| Glitch Energy | -- | 0.15 | -- | nV-s | 1 LSB change at midscale |
| AC Crosstalk | -- | -100 | -- | dB | -- |
| Supply Voltage (VDD) | 2.7 | -- | 5.5 | V | -- |
| Quiescent Current (IDD, per channel) | -- | 155 | 200 | uA | VDD = 2.7 V |
| Power-Down Current | -- | -- | 700 | nA | VDD = 5 V |
| SCLK Frequency | -- | -- | 30 | MHz | VDD = 5 V |
| SCLK Frequency | -- | -- | 20 | MHz | VDD = 2.7 V |

## Pinout

VSSOP-8 (DGK) package only.

| Pin # | Name | Direction | Description |
|---|---|---|---|
| 1 | VDD | P | Power supply input, 2.7 V to 5.5 V |
| 2 | VREF | I | Reference voltage input (shared by DAC A and DAC B) |
| 3 | VOUTB | O | Analog voltage output from DAC B |
| 4 | VOUTA | O | Analog voltage output from DAC A |
| 5 | SYNC | I | Active-low level-triggered frame-sync input (chip select). When SYNC is LOW the input shift register is enabled and data is clocked on the falling edge of SCLK; the 24-bit control+data word is executed on the 24th falling SCLK edge unless SYNC goes HIGH early (which aborts the transfer). Schmitt-trigger input. |
| 6 | SCLK | I | Serial clock input, up to 30 MHz at VDD = 5 V (20 MHz at 2.7 V). Schmitt-trigger input. |
| 7 | DIN | I | Serial data input. Clocked into the 24-bit shift register on the falling edge of SCLK. Schmitt-trigger input. |
| 8 | GND | G | Ground reference for all on-chip circuitry |

Direction key: I = Input, O = Output, P = Power, G = Ground.

**Verified against TI datasheet pin description table** (user-pasted from PDF on 2026-04-06). This supersedes an earlier incorrect version of this section that had pads 1/2/4/5/7/8 scrambled.

## Absolute Maximum Ratings

| Parameter | Min | Max | Unit |
|---|---|---|---|
| VDD to GND | -0.3 | +6.0 | V |
| VREF to GND | -0.3 | VDD + 0.3 | V |
| Digital Input Voltage to GND | -0.3 | VDD + 0.3 | V |
| VOUTA, VOUTB to GND | -0.3 | VDD + 0.3 | V |
| Operating Temperature (I grade) | -40 | +105 | deg C |
| Storage Temperature | -65 | +150 | deg C |
| Junction Temperature | -- | +150 | deg C |

Stresses beyond those listed may cause permanent damage. Exposure to absolute maximum conditions for extended periods may affect device reliability.

## Application Notes

- **SPI protocol:** The DAC8552 uses a 3-wire SPI-compatible interface (DIN, SCLK, SYNC). Data is clocked in on the falling edge of SCLK (CPOL = 0, CPHA = 1, i.e. SPI Mode 1 -- though the device also works with CPOL = 1, CPHA = 0, SPI Mode 2, since it simply clocks on the falling edge of SCLK). SYNC is active low and frames each 24-bit transaction.
- **24-bit shift register format:** Each write consists of 24 bits shifted in MSB first. Control byte (DB23–DB16): bits [23:22] unused (0); **DB21 = LDB (load/update DAC B), DB20 = LDA (load/update DAC A)**; DB19 unused; **DB18 = buffer select — 0 routes the 16-bit data to the DAC A input buffer, 1 routes it to the DAC B input buffer (THIS is the channel-select bit, not DB21)**; DB17:DB16 = power-down PD1:PD0 (00 = normal, 01 = 1 kohm to GND, 10 = 100 kohm to GND, 11 = high-Z). Bits [15:0] are the 16-bit data word. The control byte is the OR of the desired fields; the two canonical "write buffer + update output" bytes are **0x10 = write DAC A & update A** (`UPDATE_DAC_A 0x10 | BUFFER_A 0x00`) and **0x24 = write DAC B & update B** (`UPDATE_DAC_B 0x20 | BUFFER_B 0x04`). Reference: github.com/adn05/dac8552.
- **Power-on state:** On power-up, the internal registers are reset to zero and both DAC outputs are at zero-scale (0 V). The device powers up in normal operating mode (not powered down).
- **Decoupling:** Place a 1 uF to 10 uF ceramic bypass capacitor on VDD as close to the pin as possible. A 100 nF ceramic capacitor in parallel is recommended for high-frequency noise rejection. Decouple the VREF pin with a 1 uF to 10 uF ceramic capacitor to GND.
- **Reference input:** The VREF pin accepts a reference voltage from 0 V up to VDD. The output voltage of each channel is VOUT = VREF x (D / 65536), where D is the 16-bit code. For best performance, use a low-noise, low-drift precision voltage reference (such as the REF5025 also in this BOM). The reference input impedance is code-dependent; a reference buffer or low-impedance source is recommended.
